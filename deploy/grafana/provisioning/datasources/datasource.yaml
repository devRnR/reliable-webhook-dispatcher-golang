apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    uid: prometheus           # 대시보드 패널이 이 uid를 참조(webhook.json과 일치)
    isDefault: true
    jsonData:
      timeInterval: 5s
    editable: false
